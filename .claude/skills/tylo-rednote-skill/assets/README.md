# Assets Directory
Place your text content here as `.md` files.
The skill will read these as input for content generation.

## Example
Create a file like `1.md` with your raw text content, then ask Claude to process it.
